{
  "name": "sudoku-solver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:ai-backend": "cd ./backends/ai-backend && python3 run.py",
    "start:db-backend": "cd ./backends/db-backend && npm run start:dev",
    "start:client": "cd ./client && npm run start:dev",
    "shell:join": "docker exec -it sudoku-solver bash",
    "shell:start": "docker compose -f docker-compose.yml up --build",
    "shell:build": "docker-compose -f docker-compose.yml -p sudoku-solver build --no-cache",
    "shell:stop": "docker-compose -f docker-compose.yml -p sudoku-solver down",
    "shell:init": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "sharp": "^0.33.2"
  }
}
