{
  "name": "sudoku-slover",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "join": "docker exec -it sudoku-solver bash",
    "start": "docker compose -f docker-compose.yml up --build",
    "build": "docker-compose -f docker-compose.yml -p sudoku-solver build --no-cache",
    "stop": "docker-compose -f docker-compose.yml -p sudoku-solver down",
    "init": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
